<UserControl x:Class="Diallo_Business.UC_Bilans"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1000">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Historique Complet" Foreground="White" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="🔄 Actualiser" Click="BtnRefresh_Click" Margin="30,0,0,0" Width="120" Height="35" Background="#1E293B" Foreground="#38BDF8"/>
        </StackPanel>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">

            <TabItem Header="🛠 Services">
                <DataGrid x:Name="GridServices" AutoGenerateColumns="False" CanUserAddRows="False" Background="#0F172A" BorderThickness="0" Foreground="White">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding DateAction, StringFormat=dd/MM/yyyy}" Width="100"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding TypeService}" Width="*"/>
                        <DataGridTextColumn Header="Client" Binding="{Binding NomClient}" Width="*"/>
                        <DataGridTextColumn Header="Prix" Binding="{Binding Prix}" Width="100"/>
                        <DataGridTextColumn Header="Payé" Binding="{Binding Accompte}" Width="100"/>
                        <DataGridTextColumn Header="Reste" Binding="{Binding Reliquat}" Width="100"/>
                        <DataGridTemplateColumn Header="Actions" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="✏️" Click="EditService_Click" Background="Transparent" BorderThickness="0" Margin="5,0"/>
                                        <Button Content="🗑️" Click="DeleteService_Click" Background="Transparent" BorderThickness="0" Foreground="#EF4444"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="🎓 Formations">
                <DataGrid x:Name="GridFormations" AutoGenerateColumns="False" CanUserAddRows="False" Background="#0F172A">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Étudiant" Binding="{Binding NomClient}" Width="*"/>
                        <DataGridTextColumn Header="Prix" Binding="{Binding Prix}" Width="100"/>
                        <DataGridTextColumn Header="Payé" Binding="{Binding Accompte}" Width="100"/>
                        <DataGridTextColumn Header="Planning" Binding="{Binding Planification}" Width="*"/>
                        <DataGridTextColumn Header="Statut" Binding="{Binding Statut}" Width="100"/>
                        <DataGridTemplateColumn Header="Actions" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="🗑️" Click="DeleteFormation_Click" Background="Transparent" Foreground="#EF4444"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="📦 Ventes Stock">
                <DataGrid x:Name="GridVentes" AutoGenerateColumns="False" CanUserAddRows="False" Background="#0F172A">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
                        <DataGridTextColumn Header="Client" Binding="{Binding NomClient}" Width="*"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding MontantTotal}" Width="120"/>
                        <DataGridTemplateColumn Header="Actions" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="🗑️" Click="DeleteVente_Click" Background="Transparent" Foreground="#EF4444"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>